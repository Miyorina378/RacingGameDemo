# Heritage reverse cross-fade and showroom lifecycle fixes

Change make dealer navigation cross-fade in both directions and reuse one 3D showroom while car previews change. Category buttons now lose `animate-brandPop` during brand and market transitions, including fast Heritage reverse. Earlier car-sync, GPU churn, lighting, paint, Category-back, and purchase-pulse findings are addressed in traced final paths. **Watch for:** no concrete blocking showroom regression found in focused scope.

**Verdict**: APPROVED

**Blocking findings**: NO  
**Behaviorally ready**: YES

## High-level view

**Confirmed:** Heritage back set `marketBackCrossFade` before Category UI mount. Category children cannot retain pop-animation opacity while incoming footer run its reverse fade.

**Confirmed:** panel and preview share one resolved stock car. Preview swaps only stable vehicle visual; stale GLTF results cannot commit, and superseded visual resources get disposed.

**Confirmed:** reverse navigation preserve showroom through overlap, then restore normal light state. Dealer exit restore saved active paint, and purchase pulse reach live engine.

<details>
<summary>Issues (0)</summary>

No actionable blocking finding.

</details>

<details><summary>Details</summary>

### Fast Heritage reverse releases child animation fill mode

The failure required incoming Category children to keep `animate-brandPop` while their parent ran `animate-crossFadeIn`. The button guard now requires `brandCrossFadeTarget`, `brandBackCrossFade`, `marketCrossFadeTarget`, and `marketBackCrossFade` all be empty. **Confirmed:** Heritage Back sets `marketBackCrossFade`, mounts Category footer, and strips child pop animation for the reverse interval; fast forward and brand reverse get same protection.

### Car previews preserve showroom and reject stale loads

`resolvedDealerCarId` feeds both panel fallback and `previewCar`. `gameEngine.setActiveCar` keep current `GarageMode` and rebuild only stable `Vehicle` contents; generation checks discard late GLTF results and cleanup release replaced visual resources. **Confirmed:** carousel browsing no longer rebuild full showroom, and panel cannot fall back to a different car than 3D preview.

### Reverse and exit paths restore shared state

CATEGORIES call shared staged back handler, market mode clear at 400 ms boundary, and `GarageMode` restore ambient color plus normal fixtures after showroom end. Leaving dealer previews active car with `selectedColor`; purchase call use `engineRef.current.triggerPurchaseCelebration()`. **Confirmed:** old abrupt Category return, ambient tint leak, paint reset, and dead purchase pulse paths are closed.

`npx tsc --noEmit` pass. Scoped `git diff --check` only report existing trailing whitespace at `components/Game.tsx:1917`, excluded as requested lint debt. Verification is static state/render trace; no browser smoke run.

</details>

<details><summary>File map</summary>

- `components/ui/Garage.tsx` — fade guards, shared preview selection, staged Category return, exit restoration.
- `components/Game.tsx` — preview/showroom bridges and purchase pulse.
- `components/gameEngine.ts` — stable vehicle swap.
- `components/objects/Vehicle.ts` — visual generations and disposal.
- `components/modes/GarageMode.ts` — reusable showroom and normal-light restoration.

Full scoped diff: `git diff main -- components/objects/Vehicle.ts components/ui/Garage.tsx components/Game.tsx components/gameEngine.ts components/modes/GarageMode.ts`

</details>
