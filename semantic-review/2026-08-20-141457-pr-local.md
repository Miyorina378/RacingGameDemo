# Showroom visual races, Heritage transitions, and garage restoration

One reverse-transition edge remains. If market entry starts within 500 ms of the brand category screen appearing, the visit timer is canceled before recording the screen as visited. Reversing from Heritage then gives Category buttons a 550 ms eased opacity-and-translate entrance on top of the 400 ms linear parent fade, so the two views are not symmetric opacity cross-fades. **Watch for:** **confirmed** fast Heritage reverse replay Category child animation beyond the required fade.

**Verdict**: NEEDS_CHANGES

## High-level view

**Confirmed:** a fast forward selection leaves `hasVisitedCategoryRef` false, and the reverse state does not suppress `animate-brandPop`. Categories therefore move and fade for 550 ms while Heritage fades out linearly for 400 ms.

<details>
<summary>Issues (1)</summary>

1. **Medium — Fast reverse replays Categories pop** — **confirmed:** entering Heritage within 500 ms and then reversing runs the 550 ms `animate-brandPop` on Category buttons during the 400 ms parent cross-fade. Suppress the child animation whenever `marketBackCrossFade` is active, or mark the category screen visited when forward market transition begins.

</details>

<details><summary>Details</summary>

### Medium — Fast Heritage reverse is not a symmetric 400 ms opacity fade

The category visit flag is set by a 500 ms timer at `components/ui/Garage.tsx:2077-2080`. When Heritage is chosen quickly, its mode commits after 400 ms and the effect cleanup cancels that timer, leaving the flag false. On reverse, the Category button class at `components/ui/Garage.tsx:2834` checks forward targets but not `marketBackCrossFade`, so every button receives `animate-brandPop`; that animation lasts 550 ms, uses eased opacity, adds vertical translation, and uses fill mode `both`. **Confirmed:** Heritage fades out over the required 400 ms while Categories continue a different animation for another 150 ms. Add `!marketBackCrossFade` to the child-animation guard or set the visited flag as market entry starts.

</details>

<details><summary>File map</summary>

- `components/objects/Vehicle.ts` — generation-gated visual replacement and GLTF resource disposal.
- `components/ui/Garage.tsx` — contains the remaining fast-reverse Category animation regression.
- `components/Game.tsx` — preview paint restoration, theme bridge, purchase callback, vehicle visibility.
- `components/gameEngine.ts` — stable-root car rebuild and GarageMode celebration bridge.
- `components/modes/GarageMode.ts` — showroom themes, ambient/light restoration, purchase pulse, car presentation.

Full scoped diff: `git diff main -- components/objects/Vehicle.ts components/ui/Garage.tsx components/Game.tsx components/gameEngine.ts components/modes/GarageMode.ts`

</details>
